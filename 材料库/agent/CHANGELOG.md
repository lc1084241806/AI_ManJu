# Agent 修订记录(CHANGELOG)

> 本文件汇总所有 agent system prompt 的版本沿革,供人查阅用。**agent 文件本身不再包含修订记录段**——避免污染 system prompt。每次改 agent 必须同步在本文件追加条目。

格式约定:
- 每个 agent 一个二级标题,按编号字母顺序排
- 版本号按发布顺序倒序列出(最新在上)
- 条目格式:`- V<x.y>(YYYY-MM-DD):<改动摘要>`

---

## A1 小说大纲

- V1.6(2026-05-26):**新增 §推算-确认流程**。响应用户痛点「我可能只给一个想法,不想一上来填 6 个字段」。流程:(1)从用户散文式输入推算 6 字段并标置信度(高/中/低/默认/?);(2)用户逐项确认或修改,推不出来的列在 `?` 段必须补全;(3)用户回复后才进入正式 §输出 Schema。同步加异常处理(纯抽象需求 / 撞 IP / 字段未补)分支。
- V1.5(2026-05-25):**全 agent §0 硬约束统一注入**(输出格式 Markdown-only + 行为约束禁止主动续写 / 反问 / 越界)。修订记录从本文件抽出到 `材料库/agent/CHANGELOG.md`。
- V1.4(2026-05-25):D1 重命名联动——D1 三视图 → D1 视觉设定集(文字版资产卡 ↔ 视觉版设定集 对照命名)。本文件跨 agent 引用同步更新。
- V1.3(2026-05-24):全流水线 agent 编号统一重排,按新流水线顺序(②小说→③剧本→④分镜→⑤故事板→⑥视频→⑦配音)连号到 F1;D1 视觉设定集位置同步挪到分镜审查之后。本文件 agent 编号 A1 不变,跨 agent 引用统一更新。
- V1.2(2026-05-24):跨 agent 一致性修复(配合 B1 V1.2)。①题材枚举从 8 类扩为 10 类(补"悬疑"和"恐怖",与 B1 现有模块对齐);②"重生"改为"重生穿越"(与 B1 模块名对齐);③输入约定和自检清单同步更新。
- V1.1(2026-05-24):覆盖性返修。响应用户原对话链"剧情反转密集 / 每集钩子 / 5000 字 / 题材枚举"。①输入约定题材改枚举式 + 与 A4 爆点清单对齐;②核心信念加"反转密度铁律 ≥ 2 个/集";③§0.4 改"爽点类型分布"为题材专属(对齐 A4),加"反转密度"统计;④新增 §0.5 每集字数预算(100-150 字/秒);⑤硬约束加"每集反转 ≥ 2" / "题材必从枚举";⑥自检清单同步;⑦正例补全完整 8 节拍(不再用 "..." 省略),并加正例后警示。
- V1.0(2026-05-24):初版。融合用户原 prompt 链精华(三段法 + 双审核 + 防版权)+ 学习笔记/创作思维.md 的 A.1 Logline / A.2 主题 / A.4 节拍表 / A.9 铺垫回收。

## A2 小说正文

- V2.0(2026-05-25):**节奏大改 + §0 硬约束**。①输入约定从「整本一次」改为「per 1-3 集 + 强制 世界圣经」;②首次执行额外输出「世界圣经初始化版」,后续输出「世界圣经增量」;③输出 Schema 加 B 段世界圣经增量;④加 §0 输出格式 + 行为约束。修订记录抽离到 CHANGELOG.md。
- V1.3(2026-05-25):D1 重命名联动——D1 三视图 → D1 视觉设定集。本文件跨 agent 引用同步更新。
- V1.2(2026-05-24):全流水线 agent 编号统一重排,跨 agent 引用统一更新到新编号。
- V1.1(2026-05-24):内审修复。①§C 单句话规则修自相矛盾(≤12 字 vs 长台词)——改为"默认 ≤12 字,例外 ≤30 字且配画面动态,每集 ≤3 处";②节拍字数按"100-150 字/秒"上限重新校准(原节拍 2 800-1500 字超时长,改为 500-1000 字);③加 §字数预算说明,与 A1 §0.5 对齐。
- V1.0(2026-05-24):初版。基于学习笔记/创作思维.md 的 A.8 对白写作 / A.13 漫剧改编 / B.6 张力视觉化。

## A3 小说精化

- V2.1(2026-05-26):**打通 A4→A3 回炉重写回路**。①§输入约定 新增可选输入 5「A4 采纳清单」;②Part 2 新增 §回炉重写规则——以采纳项为修改目标 + 标注 `〔回炉:A4-P0-1〕` 引用 + 未采纳项不动 + 节拍铁律仍生效(动节拍要回 A1) + 回炉次数无上限;③Meta 同步输入字段。响应用户痛点「A4 只给意见不重写,我不想自己改」。
- V2.0(2026-05-25):**节奏大改 + §0 硬约束**。①输入约定从「整本一次」改为「per 1 集 + 强制 世界圣经 + A1 大纲」;②Part 2 变更标注从 HTML 注释改为 `〔精化:<原因>〕` 行尾文本(与 §0 输出格式约束一致);③新增 Part 6 世界圣经增量输出;④自检清单同步更新;⑤加 §0 输出格式 + 行为约束。修订记录抽离到 CHANGELOG.md。
- V1.4(2026-05-25):D1 V1.0 落地联动——§1.4 武器/法宝卡新增「子类型(兵器/法宝)」列,方便 D1 阶段 B 直接分流到对应模板(兵器走双状态、法宝走单状态三视图)。子类型判定规则:物理杀伤=兵器,灵力发动=法宝。
- V1.3(2026-05-25):D1 重命名联动——D1 三视图 → D1 视觉设定集(与本 agent 输出的 A3 文字资产卡 形成文字版↔视觉版对照命名)。本文件跨 agent 引用同步更新;§1 增补「A3 文字资产卡是全流水线共享 IR,D1 视觉设定集不能替代」的说明。
- V1.2(2026-05-24):全流水线 agent 编号统一重排,跨 agent 引用统一更新到新编号。
- V1.1(2026-05-24):覆盖性返修。响应用户原对话链"强化情节冲突和情感张力 / 让 xxxx 看点和爽点剧情更多 / 丰富人物分支 / 完善动物等细节"的要求。①§角色定位扩为美术指导 + 角色顾问 + **戏剧顾问**三重身份;②§硬约束 1 措辞精确化("节拍位置 / 数量 / 顺序铁律"代替"剧情骨架"模糊语);③新增 §1.6 人物分支扩展表;④§1.5 动物卡从"灵兽 / 若有"扩为含宠物 / 坐骑 / 灵兽 / 元素动物的通用动物卡;⑤新增 §Part 3 情节强化记录(冲突升级 / 看点密度 / 情感张力 / 配角参与 / 反转加速 5 类),原 §Part 3-4 顺延为 §Part 4-5;⑥自检清单 / 禁止事项同步更新。
- V1.0(2026-05-24):初版。结合 `材料库/人物/README.md` 和 `材料库/场景/README.md` 的标准模板。

## A4 内容审查

- V2.1(2026-05-26):**打通审查 → 回炉重写回路**。①新增「子报告 6 采纳指引 + 可勾选清单」——P0 默认 [x] / P1+P2 默认 [ ],引用子报告 1/2 编号,带「下一步回 A3」操作说明 + 拒绝 P0 合规层项的警示;②自检清单、禁止事项同步更新(子报告数从 4→6,加「不替用户决定采纳」铁律)。响应用户痛点「我审完不知道下一步去哪改」。
- V2.0(2026-05-25):**节奏大改 + §0 硬约束**。①输入约定从「整本一次」改为「per 5 集批审 + 强制 世界圣经 + A1 大纲」,单次上限 8 集;②输出 Schema 新增「子报告 5 世界圣经校准建议」(用户复制到世界圣经 §9);③加 §0 输出格式 + 行为约束。修订记录抽离到 CHANGELOG.md。
- V1.4(2026-05-25):D1 重命名联动——D1 三视图 → D1 视觉设定集。本文件跨 agent 引用同步更新。
- V1.3(2026-05-24):全流水线 agent 编号统一重排,跨 agent 引用统一更新到新编号。
- V1.2(2026-05-24):跨 agent 一致性修复(配合 B1 V1.2)。①题材枚举从 8 类扩为 10 类(补"悬疑"和"恐怖"题材爆点);②"重生"改为"重生穿越"(与 B1/A1 模块名对齐);③输入约定同步更新。
- V1.1(2026-05-24):内审修复。①题材爆点清单与 A1 输入约定枚举对齐(合并"战神归来"和"战神/修罗"为单一"战神";"总裁文"→"总裁";"修仙/玄幻"→"修仙玄幻";"古风/宫斗"→"古风宫斗";"复仇文"→"复仇";"甜宠/言情"→"甜宠言情");②新增"现代都市"题材爆点。
- V1.0(2026-05-24):初版。融合用户原 prompt 链的"双审核"思路,升级为强制结构化输出 + 题材专属爆点清单。

## A5 阅读版导出

- V1.0(2026-05-26):**初版**。从 A2/A3 工程稿导出读者友好稿——删节拍标签 / 删 `〔精化:...〕` 标注 / 删 HTML 注释 / 删 A3 Part 1/3/4/5/6 工程产物。零创作 / 零改写 / 零删字 / 不调段序。响应用户痛点「小说里看到节拍标签很碍眼,但 pipeline 又需要保留」——保留主流程的节拍骨架不动,旁路加阅读版导出工具。单次最多 5 集。

## B1 剧本生成

- V2.0(2026-05-25):**节奏大改 + §0 硬约束**。①输入约定从「整本一次」改为「per 1 集 + 强制 世界圣经」;②跨集一致性由世界圣经接管,本 agent 只关注本集质量;③加 §0 输出格式 + 行为约束。修订记录抽离到 CHANGELOG.md。
- V1.4(2026-05-25):D1 重命名联动——D1 三视图 → D1 视觉设定集。本文件跨 agent 引用同步更新。
- V1.3(2026-05-24):全流水线 agent 编号统一重排,跨 agent 引用统一更新到新编号。
- V1.2(2026-05-24):跨 agent 一致性修复。①题材枚举与 A1/A4 V1.1 对齐 + 补"悬疑/恐怖"使全流水线统一为 10 类(战神 / 总裁 / 修仙玄幻 / 重生穿越 / 古风宫斗 / 复仇 / 甜宠言情 / 现代都市 / 悬疑 / 恐怖);②题材模块名重命名(玄幻修仙→修仙玄幻;战神现代→战神;古风言情→古风宫斗;悬疑/推理→悬疑;恐怖/惊悚→恐怖;重生/穿越→重生穿越);③新增"总裁/复仇/甜宠言情"3 个题材模块;④§3 画外音段加下游 C1 转换预告(即梦无记忆约束)。
- V1.0(2026-05-24):初版。融合用户参考的剧本格式标准(8 大段),扩展 6 类型特化模块、集集对应、字数时长换算、强制自检。

## B2 剧本精简

- V2.0(2026-05-25):**节奏大改 + §0 硬约束**。①输入约定从「整本一次」改为「per 1 集 + 强制 世界圣经」;②加 §0 输出格式 + 行为约束。修订记录抽离到 CHANGELOG.md。
- V1.3(2026-05-25):D1 重命名联动——D1 三视图 → D1 视觉设定集。本文件跨 agent 引用同步更新。
- V1.2(2026-05-24):全流水线 agent 编号统一重排,跨 agent 引用统一更新到新编号。
- V1.1(2026-05-24):内审修复。自检清单加"中文标点防御检查",避免精简合并对白时引入英文标点。
- V1.0(2026-05-24):初版。融合用户参考的精简规则,扩展为强制结构化输出 + 镜头连接性硬约束。

## B3 剧本连贯审查

- V2.0(2026-05-25):**节奏大改 + §0 硬约束**。①输入约定从「整本一次」改为「per 5 集批审 + 强制 世界圣经 + A1 大纲 + A3 精化版」,单次上限 8 集;②加 §0 输出格式 + 行为约束。修订记录抽离到 CHANGELOG.md。
- V1.3(2026-05-25):D1 重命名联动——D1 三视图 → D1 视觉设定集。本文件跨 agent 引用同步更新。
- V1.2(2026-05-24):全流水线 agent 编号统一重排,跨 agent 引用统一更新到新编号。
- V1.1(2026-05-24):内审修复。输入约定将 A1 大纲从"建议"提升为"**必填**",对齐硬约束 §6"铺垫审必对照 A1 大纲"。
- V1.0(2026-05-24):初版。融合用户参考的"连贯性审核"思路,升级为 6 子报告结构化输出 + 优先级矩阵 + 题材爆点对接。

## C1 分镜骨架

- V2.0(2026-05-25):**节奏大改 + §0 硬约束**。①输入约定从「整本一次」改为「per 1 集 + 强制 世界圣经」;②题材枚举对齐 A1/B1(10 类);③加 §0 输出格式 + 行为约束。修订记录抽离到 CHANGELOG.md。
- V1.3(2026-05-25):D1 重命名联动——D1 三视图 → D1 视觉设定集。本文件跨 agent 引用同步更新。
- V1.2(2026-05-24):全流水线 agent 编号统一重排,跨 agent 引用统一更新到新编号。
- V1.1(2026-05-24):内审修复。①§五占位一致性正例改为不含具体颜色 / 形状,与 §13 禁止特效细节统一;②§9.2 镜头格式补"同片段内承接前镜衔接语""回忆结束色调标注""CG 质感词留给 D3";③§9.3 加"多人同时说话格式";④§13 加禁"CG 质感词";⑤§14 正例重排时长(片段 14→15s,M1/M2/M3 时长分配修正,使台词朗读时长不再被切),并加时长核算说明。
- V1.0(2026-05-24):初版。基于用户分享的"剧本→分镜"对话链,适配即梦 Seedance 2.0 物理约束,与 C2 增强 / C3 审查解耦。

## C2 分镜增强

- V2.0(2026-05-25):**节奏大改 + §0 硬约束**。①输入约定从「整本一次」改为「per 1 集 + 强制 世界圣经」;②题材枚举对齐 A1/B1(10 类);③加 §0 输出格式 + 行为约束。修订记录抽离到 CHANGELOG.md。
- V1.3(2026-05-25):D1 重命名联动——D1 三视图 → D1 视觉设定集。本文件跨 agent 引用同步更新。
- V1.2(2026-05-24):全流水线 agent 编号统一重排,跨 agent 引用统一更新到新编号。
- V1.1(2026-05-24):内审修复。①§1.4 战神特效偏好"气势光晕"补具体颜色(白金色),与 §1.2 必含 4 属性统一;②§2.2 色温表"暗红/深紫/漆黑"补数值,与 §2.3 格式统一;③新增 §6.4 死锁回退机制(扩片段时长 / 特效降级 / 回剧本端缩台词);④§7.1 增强清单举例 P1-M2 改为 P3-M5(避免与 §11 正例"P1-M2 反应镜头不该加特效"冲突),并加警示。
- V1.0(2026-05-24):初版。针对即梦 Seedance 2.0 适配,与 C1 骨架解耦,负责"特效 / 色温 / 景别 / 运镜 / 特写 + 时间线重排"。

## C3 分镜审查

- V2.0(2026-05-25):**节奏大改 + §0 硬约束**。①输入约定从「整本一次」改为「per 1 集 + 强制 世界圣经」;②题材枚举对齐 A1/B1(10 类);③加 §0 输出格式 + 行为约束。修订记录抽离到 CHANGELOG.md。
- V1.3(2026-05-25):D1 重命名联动——D1 三视图 → D1 视觉设定集。本文件跨 agent 引用同步更新。
- V1.2(2026-05-24):全流水线 agent 编号统一重排,跨 agent 引用统一更新到新编号。
- V1.1(2026-05-24):内审修复。①审查从 3 层扩为 4 层,新增"子报告 4 镜头/片段连贯性审"(同片段衔接 / 跨片段复述 / 跨场景转场 / 占位一致 / 回忆色调成对);②§3.4 内心独白审从 2 项扩为 4 项(加内心独白格式 / 角色音色);③新增 §3.5 画外音残留全文扫描;④台词时长审正例从 C1 P1-M3 改为独立 P5-M2(避免与 C1 正例数据冲突误导教学)。
- V1.0(2026-05-24):初版。基于用户分享的"剧本→分镜"对话链第 10-13 步合并,适配即梦 Seedance 2.0 + 抖音合规。

## D1 视觉设定集(三视图)

- V1.3(2026-05-25):**§0 硬约束统一注入**(输出格式 + 行为约束)。修订记录抽离到 CHANGELOG.md。
- V1.2(2026-05-25):agent 编号大改联动。全流水线编号体系由"A1-A14 + A11b 平铺"改为"按阶段字母分类"(A 小说 / B 剧本 / C 分镜 / D 故事板 / E 视频 / F 配音),本 agent 由 `A11_视觉设定集` 重命名为 `D1_视觉设定集`(`git mv` 保留历史);文件内所有跨 agent 引用按机械替换全量更新。同步建成配套 SOP `材料库/SOP/故事板SOP.md`。
- V1.1(2026-05-25):三项重大调整。①新增 `{画风}` 变量 + 风格包注入机制(来自 `材料库/画风/<画风>.md`),阶段 A 流程从 4 步扩为 5 步;默认画风为"国漫CG_老师标准"则跳过注入。②阶段 B 从"一次性出全部"拆为 7 批次(0 元素清点 / 1 人物 / 2 兵器 / 3 神兽动物 / 4 法宝 / 5 道具 / 6 场景),硬约束"一次只跑一批"防止上下文塞爆。③输出从单文件改为按模块分文件存档。④把"两轮自审 + 终审清点"全部拆出到独立 agent **D2 视觉设定集审查**——本 agent 只生成不自审。
- V1.0(2026-05-25):初版。基线模板源自用户老师 2026-05-25 提供的"小说三视图标准"(人物四视图 / 兵器双状态 / 神兽妖兽动物 / 法宝灵宝 / 道具 / 场景 共 6 模块)。两阶段工作流(A 定型 + B 抽取)落地老师 9 步交互流程。题材分流表按"纯古风 / 玄幻 仙侠 / 现代 / 科幻"四档定义模块开关。

## D2 视觉设定集审查

- V1.2(2026-05-25):**§0 硬约束统一注入**(输出格式 + 行为约束)。修订记录抽离到 CHANGELOG.md。
- V1.1(2026-05-25):agent 编号大改联动。本 agent 由 `A11b_视觉设定集审查` 重命名为 `D2_视觉设定集审查`(`git mv` 保留历史);文件内跨 agent 引用全量更新。
- V1.0(2026-05-25):初版。从 D1 V1.0 中拆出"两轮自审 + 终审清点"段独立为审查 agent,对齐 A4/B3/C3 的"生成 vs 审查分离"设计原则。审查清单按老师 9 步流程的步骤 3-4(人物模块审 + 其他模块审)+ 步骤 8(终审遗漏)落地。

## D3 故事板关键帧 prompt

- V0.5(2026-05-26):**脚手架版,零实战**。响应仓库 review 反馈「下游 1/3 流水线断头」。schema 含每镜首帧 prompt(中文+英文双语)、锚定参考(D1 视觉设定集)、画风后缀、即梦适配。**正例 / 反例为推测性占位**,第一次实战后必须用真实案例覆盖、升 V1.0。

## E1 图生视频 prompt

- V0.5(2026-05-26):**脚手架版,零实战**。schema 含每镜运动 prompt(中文+英文双语)、C2 运镜词机械复制、即梦 Seedance 2.0 物理约束(单镜 ≤5s / 镜头运动与角色运动不同时大动)、工具适配后缀表(即梦 / 可灵 / Runway / Pika)。**正例 / 反例为推测性占位**,第一次实战后必须用真实案例覆盖、升 V1.0。

## F1 配音指令

- V0.5(2026-05-26):**脚手架版,零实战**。schema 含每句台词 TTS 指令(语速 5 档 + 数值 / 语气 / 情绪强度 1-5 级 / `[短停] [长停] [呼吸]` 停顿标注 / 重音)、音色锚定(`材料库/人物/<角色>.md` §音色)、{TTS 工具} 专属参数(ElevenLabs / 魔音工坊 / Fish Audio / 剪映)。**正例 / 反例为推测性占位**,第一次实战后必须用真实案例覆盖、升 V1.0。
