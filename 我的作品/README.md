# 我的作品

> 你做过的每一部 AI 漫剧,都装在这里。
> 一部作品一个子目录,内部按 10 步流水线组织。

---

## 命名规范

```
我的作品/<编号>_<作品名>/
```

例:
- `000_练手作/`(阶段 1 的端到端练手)
- `001_战神归来/`
- `002_重生之我是太子妃/`

**编号建议**:三位数,从 `000` 开始递增。这样目录排序就是时间顺序。

---

## 单部作品的标准结构

```
<编号>_<作品名>/
  00_企划.md             ← 题材 / 受众 / 平台 / 集长 / 卖点
  01_小说.md             ← 完整小说(A1-A4 产出);A3 顺手输出的文字资产卡沉淀到 材料库/人物 与 材料库/场景
  02_剧本.md             ← 分场剧本(B1-B3 产出)
  03_分镜.md             ← 镜头清单 + 全角色服装/道具清单(C1-C3 产出)
  04_视觉设定集/         ← D1 产出:三视图 / 关键道具 reference / 服装变化集(.png,大文件不入 git)
    角色_<角色名>/
      front.png side.png back.png
      变装_<场景>.png
    道具_<物品名>.png
  05_关键帧/             ← D3 产出:每镜首帧出图(.png,大文件不入 git)
    1_1.png
    1_2.png
    ...
  06_视频/               ← E1 图生视频成果(.mp4,不入 git)
    1_1.mp4
    1_2.mp4
    ...
  07_音频/               ← F1 配音 + BGM + 音效(.wav .mp3,不入 git)
    配音/
    bgm/
    音效/
  08_成片/               ← 最终视频(.mp4,不入 git)
    final.mp4
    final_竖版.mp4
  09_复盘.md             ← 做完后 24h 的数据复盘
```

> **没有独立的 02_设定集.md(V1.2 调整)**:设定集分两种形态——
> - **文字版**(角色/场景/道具描述)由 A3 在小说阶段顺手产出,**直接沉淀到 `材料库/人物/`、`材料库/场景/`** 复用,不在本作品目录留副本
> - **视觉版**(三视图、关键道具 reference)由 D1 视觉设定集做,在分镜锁定后,产物放本目录 `04_视觉设定集/`

---

## 各文件的标准模板(可复制)

### 00_企划.md

```markdown
# 企划 · <作品名>

## 题材
-

## 受众画像
- 年龄:
- 性别:
- 兴趣:
- 痛点 / 共鸣点:

## 平台 + 集长
- 平台:
- 集长:60s / 180s / 300s
- 计划集数:

## 卖点(3 个)
1. 钩子:
2. 爽点 / 情绪:
3. 差异化:

## 竞品分析
- 同题材头部 3 名:
- 他们的强项:
- 我的切入点:

## 计划制作周期
- 启动日期:
- 预计完成:
- 实际完成:

## 用到的资产(从材料库)
- 角色:[引用 材料库/人物/ 下的哪些]
- 场景:[引用 材料库/场景/ 下的哪些]
- Agent:[用了 A几-A几]
```

### 09_复盘.md

```markdown
# 复盘 · <作品名>

## 制作复盘

### 时间复盘
- 各步骤耗时:(对比 SOP)
- 卡点:
- 提速点:

### 质量复盘
- 最满意的镜头:
- 最不满意的镜头:
- 解决了哪些难点:
- 暴露了哪些新难点:

## 数据复盘(发布 24h 后填)

### 核心指标
- 播放:
- 完播率:
- 3 秒完播率:
- 点赞 / 评论 / 分享:
- 关注转化:
- 流量来源(推荐 / 关注 / 搜索)占比:

### 命中点
- 评论里的好评关键词:
- 流量爆发节点(是发布几小时后):
- 最多人停留的镜头(看完播曲线):

### 没命中点
- 在第几秒掉播放最多:
- 评论里的吐槽点:
- 我的判断:

## 下一部要做的调整
- 题材方向:
- 标题调整:
- 钩子调整:
- 镜头节奏:
- 画风:
- 配音 / BGM:

## 沉淀到材料库
- 新增的角色 → `材料库/人物/`:
- 新增的场景 → `材料库/场景/`:
- 新解法 → 更新到哪个文件:
- Agent 修订 → 哪个 Agent 升级了:
```

---

## 大文件管理

`.gitignore` 已经配置:
- `*.mp4` `*.mov` `*.mkv` `*.mp3` `*.wav` `*.psd` 全部不入 git
- 这些文件**只在本地保留** + 自己备份(网盘 / 移动硬盘)

文本类(.md)入 git,可以追溯历史、跨设备同步。

---

## 与"材料库"的协作

```
材料库/(可复用资产)
  ↓ 引用
我的作品/<项目>/(具体作品)
  ↓ 反哺
材料库/(更新解法 / 添加新角色 / 修订 agent)
```

### 启动新作品时
1. 在 `00_企划.md` 里写明用到哪些角色 / 场景(从材料库引用)
2. 用 `材料库/agent/` 里的 agent 跑各步骤
3. 把产出存到本目录对应文件

### 完成新作品时
1. 把新创建的角色 / 场景沉淀回 `材料库/人物/` 或 `材料库/场景/`
2. 把改进后的 agent 版本更新到 `材料库/agent/`
3. 写 `09_复盘.md`,记录"沉淀到材料库的内容"

**作品越多,材料库越厚,后续作品越快。**

---

## 第一部作品:000_练手作

完成阶段 1(全流程鸟瞰)的端到端练手作放在 `000_练手作/`:
- 不追求质量,只追求"跑通"
- 完成后写一份简单的 09_复盘.md
- 这部作品的目的是**让你知道每一步真实的工作量和卡点**
